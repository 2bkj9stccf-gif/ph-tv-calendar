#!/bin/bash
cd "$(dirname "$0")"
echo "Copying upcoming episodes into your master catalog (add-only, safe)…"
echo
python3 backfill_next_episodes.py
echo
echo "Done — switch back to Claude and paste this whole window."
